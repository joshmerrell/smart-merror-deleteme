---
---
<script>
const video = document.querySelector("video");
const img = document.querySelector("img.fallback");

if (navigator.mediaDevices.getUserMedia) {
  
  navigator.mediaDevices
	.getUserMedia({
	  video: true
	})
	.then((stream) => (video.srcObject = stream))
	.catch((error) => {
		console.error(error);
		img.classList.remove("hidden");
	});
}
</script>


<!-- Webcam Video -->
<video autoplay></video>
<img src="/fallback.png" class="fallback hidden" width="800" height="600" alt="">